
const initialState = {
    apiResult: [],
    change: 0,
    lastRate: 0
};

const rootReducer = (state = initialState, action) => {
    // console.log(state);
    return state;
};

export default rootReducer;
